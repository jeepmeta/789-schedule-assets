# 789 Schedule Widget

This repository contains a React-based schedule widget and a local "studio mixing board" admin panel. The widget is driven by `src/config.json` which is the single source of truth for assets, schedule, and visual overrides.

## Quick start

1. Install dependencies:
   ```bash
   npm install
   npm start
   npm run deploy
 
 ## Embed Code

 